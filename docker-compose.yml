services:
  # 使用 Cloudflare Tunnel 部署 (build 和 up 必须分开执行):
  #   docker compose --profile tunnel build
  #   docker compose --profile tunnel up -d
  #
  # 仅重新部署 CF Worker (不重启 processor):
  #   docker compose run --rm deploy
  deploy:
    build:
      context: .
      dockerfile: Dockerfile.deploy
    env_file: .env
    environment:
      - CLOUDFLARE_API_TOKEN=${CLOUDFLARE_API_TOKEN:-}
      - CLOUDFLARE_ACCOUNT_ID=${CF_ACCOUNT_ID:-}
    restart: "no"

  # VPS 本地处理服务 (大文件 + 媒体处理)
  # Tunnel 和 Caddy 均通过 Docker 内部网络访问, 无需暴露宿主机端口
  # 调试时如需直连: docker compose run -p 3000:3000 processor
  processor:
    build: ./processor
    restart: unless-stopped
    environment:
      - PORT=3000
      - TG_BOT_TOKEN=${TG_BOT_TOKEN}
      - AUTH_SECRET=${VPS_SECRET}
      - TG_LOCAL_API=${TG_LOCAL_API:-https://api.telegram.org}
      - DEFAULT_CHAT_ID=${DEFAULT_CHAT_ID}
      - TEMP_DIR=/tmp/tg-s3
    volumes:
      - processor-data:/tmp/tg-s3
    deploy:
      resources:
        limits:
          memory: 2G

  # Cloudflare Tunnel: 安全暴露 processor 给 CF Worker, 无需开放公网端口
  # 启用方法: 在 .env 中设置 CF_TUNNEL_TOKEN, 然后:
  #   docker compose --profile tunnel up -d
  # 或由 deploy.sh 自动创建 (需要 CF_CUSTOM_DOMAIN)
  tunnel:
    image: cloudflare/cloudflared:latest
    restart: unless-stopped
    command: tunnel run
    environment:
      - TUNNEL_TOKEN=${CF_TUNNEL_TOKEN}
    depends_on:
      - processor
    profiles:
      - tunnel

  # Optional: Telegram Local Bot API Server for 2GB file support
  # telegram-bot-api:
  #   image: aiogram/telegram-bot-api:latest
  #   restart: unless-stopped
  #   environment:
  #     - TELEGRAM_API_ID=${TELEGRAM_API_ID}
  #     - TELEGRAM_API_HASH=${TELEGRAM_API_HASH}
  #     - TELEGRAM_LOCAL=1
  #   ports:
  #     - "127.0.0.1:8081:8081"
  #   volumes:
  #     - tg-bot-api-data:/var/lib/telegram-bot-api

volumes:
  processor-data:
  # tg-bot-api-data:
