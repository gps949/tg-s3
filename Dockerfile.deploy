FROM node:22-slim

RUN apt-get update && apt-get install -y --no-install-recommends \
    curl openssh-client rsync git \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app
COPY package.json ./
RUN npm install

COPY . .

RUN chmod +x deploy.sh

ENTRYPOINT ["./deploy.sh"]
